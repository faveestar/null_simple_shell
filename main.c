#include "team_fa_lib.h"

/**
* main -This function is the entry point for the simple shell.
*
* Return: Always 0 (success).
*/
int main(void)
{
process_user_input();

_printf("Exiting team_fa shell...\n");
return (EXIT_SUCCESS);
}
